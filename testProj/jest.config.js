module.exports = {
  // preset: 'react-native', // Use the react-native preset
  // preset: 'ts-jest', // If you're using TypeScript
  transform: {},
  // transformIgnorePatterns: [
  //   '/node_modules/(?!yoga-layout)', // Exclude `yoga-layout` from being ignored
  // ],
};
